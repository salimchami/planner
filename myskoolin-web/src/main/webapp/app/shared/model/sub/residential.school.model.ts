export class ResidentialSchool {

    constructor(
        public registered?: boolean,
        public name?: string,
        public periodStart?: Date,
        public periodEnd?: Date,
    ) {
    }
}
