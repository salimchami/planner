export class Newsletter {

    private email: string;
    private langKey: string;

    constructor(email: string, langKey: string) {
        this.email = email;
        this.langKey = langKey;
    }
}
